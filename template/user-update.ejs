<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%- include('css.ejs') %>
</head>
<body>        
    <form action="/user/<%= user.id %>?_method=PUT" method="POST">
        <div>
            아이디 :
            <input type="text" name="id" value="<%= user.id %>">
        </div>
        <div>
            비밀번호 :
            <input type="password" name="pwd" value="<%= user.pwd %>">
        </div>
        <div>
            이름 :
            <input type="text" name="name" value="<%= user.name %>">
        </div>
        <div>성별:
            <input type="radio" name="gender" value="M" id="M" <%= user.gender === "M" ? "checked": "" %> ><label for="M">남</label>
            <input type="radio" name="gender" value="F" id="F" <%= user.gender === "F" ? "checked": "" %> ><label for="F">여</label>
        </div>
        <button type="submit">수정</button>
    </form>
</body>
</html>